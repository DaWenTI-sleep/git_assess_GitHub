# 复制为项目根目录 .env 后填写（.env 勿提交 Git）

# 服务端口
SERVER_PORT=8080
FRONTEND_PORT=5173

# 前端 Vite 将 /api 代理到本机后端
BACKEND_TARGET=local
BACKEND_LOCAL_HOST=127.0.0.1
BACKEND_LOCAL_PORT=8080

# 本机 MySQL（先建库并执行 sql/as_v0.1.1_260522.sql，按本机实际情况填写）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=lab_assess
DB_USERNAME=root
DB_PASSWORD=你的本机密码
DB_TIMEZONE=Asia/Shanghai
DB_JDBC_PARAMS=useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true

# JPA
JPA_SHOW_SQL=true

# CORS 允许来源（逗号分隔）
CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:*,http://127.0.0.1:*

# 可选：.env 不在默认位置时，设为 .env 的绝对路径
# DOTENV_PATH=
